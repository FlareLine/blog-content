name: Deployment workflow
on:
  push:

jobs:
  cdk:
    name: Check template
    runs-on: ubuntu-20.04
    env:
      AWS_ACCESS_KEY: ${{ secrets.AWS_ACCESS_KEY }}
      AWS_SECRET_KEY: ${{ secrets.AWS_SECRET_KEY }}
      AWS_REGION: ${{ secrets.AWS_REGION }}
      AWS_CONTENT_BUCKET: ${{ secrets.AWS_CONTENT_BUCKET }}
    steps:
      - name: Checkout project
        uses: actions/checkout@v2
      - name: Synth template
        run: cd deploy && npm install && ./node_modules/aws-cdk/bin/cdk synth --path-metadata false --version-reporting false
  deploy:
    name: Deploy to AWS
    runs-on: ubuntu-20.04
    if: github.ref == 'refs/heads/main'
    env:
      AWS_ACCESS_KEY: ${{ secrets.AWS_ACCESS_KEY }}
      AWS_SECRET_KEY: ${{ secrets.AWS_SECRET_KEY }}
      AWS_REGION: ${{ secrets.AWS_REGION }}
      AWS_CONTENT_BUCKET: ${{ secrets.AWS_CONTENT_BUCKET }}
    steps:
      - name: Checkout project
        uses: actions/checkout@v2
      - name: Upload content artifact
        uses: actions/upload-artifact@v2
        with:
          name: content
          path: content/*.md
      - name: Run deployment
        run: cd deploy && npm install && ./node_modules/aws-cdk/bin/cdk deploy